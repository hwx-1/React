import { HtmlReport } from './html';
import { Summary } from '../coverage';
export declare class ReportNode {
    private _date;
    private _name;
    private _children;
    private _father;
    private _absolutePath;
    private _relativePath;
    private _HtmlReport;
    constructor(name: string, summary: Summary, absolutePath: string, date: string);
    get date(): string;
    set date(value: string);
    get HtmlReport(): HtmlReport;
    set HtmlReport(value: HtmlReport);
    get htmlReport(): HtmlReport;
    set htmlReport(value: HtmlReport);
    get name(): string;
    set name(value: string);
    get children(): Map<string, ReportNode>;
    set children(value: Map<string, ReportNode>);
    get father(): any;
    set father(value: any);
    get absolutePath(): string;
    set absolutePath(value: string);
    get relativePath(): string;
    set relativePath(value: string);
    addFather(father: ReportNode): void;
    addChild(child: ReportNode): void;
    removeChild(child: ReportNode): void;
}
