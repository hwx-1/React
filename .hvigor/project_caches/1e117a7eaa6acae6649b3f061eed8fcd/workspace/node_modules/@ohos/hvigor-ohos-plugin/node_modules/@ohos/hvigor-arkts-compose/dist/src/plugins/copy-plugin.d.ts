interface Ignore {
    ignored: (p: {
        name: string;
    }) => boolean;
}
interface Target {
    src: string;
    dest: string;
    rename?: string | Function;
    ignore?: Ignore;
    onlyFiles?: boolean;
}
interface CopyOptions {
    copyOnce?: boolean;
    hook?: string;
    targets?: Target[];
}
export default function copyPlugin(copyOptions?: CopyOptions): {
    [x: string]: string | (() => Promise<void>);
    name: string;
};
export {};
