"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadTemplate = exports.relative = void 0;
const path = require("path");
const fs = require("fs");
function relative(from, to) {
    let relativePath = path.relative(from, to);
    return path.posix.join(...relativePath.split(/\\/));
}
exports.relative = relative;
function loadTemplate(fileName) {
    let template = path.join(__dirname, '../../../../res/template', fileName);
    if (!fs.existsSync(template)) {
        template = path.join(__dirname, fileName);
    }
    return fs.readFileSync(template, { encoding: 'utf-8' });
}
exports.loadTemplate = loadTemplate;
